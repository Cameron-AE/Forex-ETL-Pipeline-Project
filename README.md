
This project demonstrates an end-to-end ETL pipeline for real-world forex data.
The pipeline extracts raw CSV data ,cleans and transforms it into a consistent long format, and loads it into a SQLite3 database for analysis
TECH STACK:
-Languages:Python
-Libraries: Pandas, SQLite3
-Tools: VS Code
